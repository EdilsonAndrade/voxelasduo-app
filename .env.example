# String de conexão do cluster MongoDB Atlas (tier gratuito M0).
# Nunca commitar o valor real — preencha em .env.local (dev) e nas
# variáveis de ambiente do projeto na Vercel (produção).
MONGODB_USERNAME=
MONGODB_PASSWORD=
MONGODB_URI=